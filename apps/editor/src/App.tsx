/**
 * Slice 3 scaffold placeholder: proves the Vite+React+Tauri shell boots and
 * the rusqlite catalog IPC commands exist. The real catalog browser and map
 * viewer are wired in Commit B (see CatalogBrowser.tsx / MapViewer.tsx).
 */
export function App() {
  return (
    <div style={{ padding: 24 }}>
      <h1>ThreeMaker Editor</h1>
      <p>Scaffold booted — catalog browser and map viewer land in the next commit.</p>
    </div>
  );
}
