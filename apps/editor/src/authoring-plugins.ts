/**
 * Editor re-export of the shared parse-only registry.
 * New authoring plugins are registered in `@threemaker/core` (`authoring-registry.ts`).
 */

export { authoringPlugins } from '@threemaker/core';
